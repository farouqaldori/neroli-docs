---
title: "Isolated worktrees"
description: "Understand repositories, workspaces, sessions, branches, worktrees, and panes."
---

You do not need to think about Git worktrees to use Neroli, but they explain why
parallel sessions stay manageable. Each session gets a real checkout with its
own branch, files, running processes, panes, and diff.

<Frame caption="A Neroli workspace can contain many isolated sessions, each with its own branch, panes, and review decision.">
  <div className="not-prose rounded-xl border border-dashed border-gray-300 bg-gray-50 p-8 text-center text-sm text-gray-600 dark:border-gray-700 dark:bg-gray-900 dark:text-gray-300">
    Placeholder image: one repository workspace with three session lanes, each showing branch, panes, changed files, and review status.
  </div>
</Frame>

## Core model

| Term | What it means in Neroli |
| --- | --- |
| Repository | A Git codebase that Neroli can open. |
| Workspace | The Neroli entry for a repository. It contains sessions, worktrees, actions, and panes. |
| Worktree | A separate checkout for one stream of work. It usually maps to one branch. |
| Session | An agent run inside a worktree. A session can be Claude, Codex, shell, or another supported runtime. |
| Pane | A visible surface attached to a session, such as terminal, browser, simulator, diff, or logs. |
| Action | A repo-owned automation that can run manually or on lifecycle events. |

## What isolation gives you

- Changes stay in the worktree's directory and branch.
- Commands run from the worktree path.
- Browser and simulator panes stay attached to the session that opened them.
- Diffs and review state stay scoped to the current worktree.
- You can archive or remove a worktree without touching unrelated sessions.

<Warning>
  A worktree is not a security sandbox. Agents and commands can still access what your macOS user can access.
</Warning>

## Branches and review

The branch is the review unit. Treat each branch as something you may inspect, test, merge, or discard.

Good branch boundaries:

- One feature or fix.
- One experiment.
- One pull request.
- One reviewable chunk of a larger project.

Poor branch boundaries:

- Unrelated changes for different product areas.
- A test-only cleanup mixed with a risky feature.
- Multiple competing approaches in the same branch.

## Existing branches

You can use Neroli with existing Git worktrees and branches. If a branch is
already checked out elsewhere, Git will not let a second worktree check it out
at the same time.

Use a new branch name when you want a second isolated attempt from the same starting point.

## Where to go next

<CardGroup cols={2}>
  <Card title="Workflow" icon="route" href="/concepts/workflow">
    See how a session moves from prompt to review.
  </Card>

  <Card title="Git worktrees" icon="git-branch" href="/concepts/git-worktrees">
    Learn the Git model behind Neroli's isolation.
  </Card>
</CardGroup>
